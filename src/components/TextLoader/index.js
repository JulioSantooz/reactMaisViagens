import React from "react";
import './style.css';

function TextLoader() {
    return(
        <section className="text-lazy-loading">
            <h1>Carregando...</h1>
        </section>
    );
}

export default TextLoader;